# Install — llm-selector

Standalone:

```bash
cp -r skills/llm-selector/ /path/to/your/project/skills/
```

Slash trigger:

```bash
cp skills/llm-selector/commands/llm-selector.toml ~/.claude/commands/
```

Verify:

```bash
python -m skills.llm-selector --task code
```
