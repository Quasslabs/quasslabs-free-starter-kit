# Functions — llm-selector

## Pure functions (Lambda-ready)

| Function | Input | Output | Notes |
|---|---|---|---|
| `select(task_type, budget=0.0)` | str, float | dict | Returns primary + fallback + cost |
| `catalog()` | — | dict | Returns the full task-type table |

## AI-assisted steps

None.

## External services

None.
