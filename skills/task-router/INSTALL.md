# Install — task-router

Standalone:

```bash
cp -r skills/task-router/ /path/to/your/project/skills/
```

Slash trigger:

```bash
cp skills/task-router/commands/task-router.toml ~/.claude/commands/
```

Verify:

```bash
python -m skills.task-router "deploy to prod"
```
