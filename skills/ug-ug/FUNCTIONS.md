# Functions — ug-ug

## Pure functions (Lambda-ready)

| Function | Input | Output | Notes |
|---|---|---|---|
| `compress_text(text, level)` | str, str | str | Applies level rules; deterministic |
| `strip_banned_openers(text)` | str | str | Regex pass over leading sentences |
| `is_compressible(context)` | dict | bool | Returns False for code blocks + client deliverables |

## AI-assisted steps

None. All transforms are rule-based.

## External services

None.
